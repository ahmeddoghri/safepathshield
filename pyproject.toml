[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "safepathshield"
version = "0.1.0"
description = "Audio-video offset and drift repair tool"
readme = "README.md"
requires-python = ">=3.10"
license = {text = "MIT"}
authors = [{name = "Ahmed Doghri"}]
dependencies = []

[project.scripts]
safepathshield = "safepathshield.cli:main"

[tool.setuptools]
packages = ["safepathshield"]

[tool.setuptools.package-data]
safepathshield = ["web/*.html"]
